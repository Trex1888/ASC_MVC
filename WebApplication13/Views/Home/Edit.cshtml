@model Location

@using (Html.BeginForm("Edit", "Home", FormMethod.Post))
{
    @Html.HiddenFor(model => model.LocationGuid)
    <div class="form-group">
        @Html.LabelFor(model => model.LOCATION_NAME)
        @Html.EditorFor(model => model.LOCATION_NAME, new { htmlAttributes = new { @class = "form-control", style = "width: 50%;" } })
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.LOCATION_ID)
        @Html.EditorFor(model => model.LOCATION_ID, new { htmlAttributes = new { @class = "form-control", style = "width: 50%;" } })
    </div>
    <div class="form-check">
        @Html.LabelFor(model => model.IS_CLEARANCE, htmlAttributes: new { @class = "form-check-label" })
        @Html.CheckBoxFor(model => model.IS_CLEARANCE, new { @class = "form-check-input" })
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    @Html.ActionLink("Cancel", "Index", null, new { @class = "btn btn-danger ml-2" })
}
